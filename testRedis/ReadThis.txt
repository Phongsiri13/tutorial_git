# redis
For dev
# start server
redis-server --port 6739

# access to the redis database
redis-cli -p 6739

# graphQL
{
  "query": "{ employees(limit: 8, gender: \"Male\") { id firstName age gender } }"
}
type Query {
    employee(id: ID!): Employee
    employees(limit: Int = 6, gender: String, age: AGE): [Employee]
}
enum AGE{
    YOUNG
    OLD
}
